Given(/^the file data\/example\.rb exists$/) do
  raise "DIR #{Dir['*']}"
  pending # express the regexp above with the code you wish you had
end

When(/^I run `redund csv Def data > out\.csv`$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^a file named out\.csv is created$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^the file out\.csv contains the redundancy of all the methods of example\.rb$/) do
  pending # express the regexp above with the code you wish you had
end